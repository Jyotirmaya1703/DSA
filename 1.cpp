#include<bits/stdc++.h>
#include<algorithm>
// #include<list>
using namespace std;

int main(int argc, char const *argv[])
{
    // vector<int> v;
    // v.push_back(1);
    // v.push_back(2);
    // v.push_back(3);
    // v.push_back(4);
    // v.push_back(4);
    // v.push_back(4);
    // v.push_back(4);
    // v.push_back(8);

    // int a = lower_bound(v.begin(), v.end(), 4)-v.begin();
    // int b = upper_bound(v.begin(), v.end(), 4)-v.begin();
    // cout<<a<< " " << b;

    string a = "abcd";
    reverse(a.begin(), a.end());
    cout<<a;

    return 0;
}
